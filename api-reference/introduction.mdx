---
title: 'Introduction'
description: 'What this API reference section covers'
---

## Welcome

This section documents the FluxPrompt MCP server's HTTP surface: OAuth consent
and token exchange, workspace-scoped MCP token management, and the internal
endpoints the MCP server itself calls back into the main API.

<Card title="FluxPrompt MCP" icon="plug" href="/mcp/overview">
  Start here for what the MCP server is and how it connects to a workspace.
</Card>

For the platform's regular REST API — the one authenticated with an `api-key`
header — see [External API endpoints](/main-api/specific-features/external-api-endpoints).
